from setuptools import setup
from ciphrtxt import __version__

setup(name='ciphrtxt',
      description='ciphrtxt core library providing definition of core data types',
      version=str(__version__),
      author='Joseph deBlaquiere',
      author_email='jadeblaquiere@yahoo.com',
      packages=['ciphrtxt'])
